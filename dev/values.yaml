replicaCount: 3
image:
  repository: us-central1-docker.pkg.dev/turnkey-clover-405400/artemis/artemis
  pullPolicy: IfNotPresent
  tag: "2.0.0"

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: dev-artemis.letstouchthesky.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: 
   - secretName: chart-artemis-tls
     hosts:
       - dev-artemis.letstouchthesky.com 